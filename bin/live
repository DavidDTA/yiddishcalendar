#! /bin/bash

set -e

workspace=build/bin/live
rm -rf "${workspace}"
mkdir -p "${workspace}"
cp -r src/static/* "${workspace}"
elm-live src/elm/Main.elm --open --dir="${workspace}" --port=8002 -- --output="${workspace}/-/index.js" --debug
